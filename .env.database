# DawsOS Database Configuration
# Created: 2025-10-23
# Purpose: Database connection strings for different roles

# Production Application Role (non-superuser, RLS enforced)
# Use this for all application code
DATABASE_URL=postgresql://dawsos_app:dawsos_app_pass@localhost:5432/dawsos

# Admin Role (superuser, for migrations and admin tasks)
# Use this ONLY for database migrations and admin operations
DATABASE_URL_ADMIN=postgresql://dawsos:dawsos@localhost:5432/dawsos

# Note: RLS works correctly with dawsos_app role (non-superuser)
# The dawsos superuser bypasses RLS unless FORCE ROW LEVEL SECURITY is set
